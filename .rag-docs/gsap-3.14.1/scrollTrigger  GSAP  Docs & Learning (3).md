-   [ScrollSmoother](https://gsap.com/docs/v3/Plugins/ScrollSmoother/)
-   properties
-   .scrollTrigger

### .scrollTrigger : ScrollTrigger

The ScrollTrigger instance that ScrollSmoother created internally to manage the smooth scrolling effect of the page.

### Details[](https://gsap.com/docs/v3/Plugins/ScrollSmoother/scrollTrigger/#details "Direct link to Details")

The [ScrollTrigger](https://gsap.com/docs/v3/Plugins/ScrollTrigger) instance that ScrollSmoother created internally to manage the smooth scrolling effect of the page.

```bash
let smoother = ScrollSmoother.create({...});let nativeScrollVelocity = smoother.scrollTrigger.getVelocity();
```

[

Previous

.progress

](https://gsap.com/docs/v3/Plugins/ScrollSmoother/progress)[

Next

.vars

](https://gsap.com/docs/v3/Plugins/ScrollSmoother/vars)