### revert( ) ;

Reverts to the original content (the innerHTML before the split).

### Details[](https://gsap.com/docs/v3/Plugins/SplitText/revert()/#details "Direct link to Details")

Reverts to the original content (the `innerHTML` before the split) and also calls `kill()` internally.